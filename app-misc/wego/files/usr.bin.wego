#include <tunables/global>

/usr/bin/wego {
  #include <abstractions/base>

  /etc/host.conf r,
  /etc/hosts r,
  /etc/nsswitch.conf r,
  /etc/passwd r,
  /etc/resolv.conf r,
  /etc/ssl/certs/ca-certificates.crt r,
  owner @{HOME}/.wegorc rw,
  /proc/sys/net/core/somaxconn r,
  /usr/bin/wego mr,
}
